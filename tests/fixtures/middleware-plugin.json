{
  "$schema": "https://www.krakend.io/schema/v3.json",
  "version": 3,
  "port": 18080,
  "extra_config": {
    "plugin/middleware": {
      "pattern": "plugins/*.so",
      "name": []
    }
  },
  "endpoints": [
    {
      "endpoint": "/hello",
      "backend": [
        {
          "host": ["http://127.0.0.1:8081"],
          "url_pattern": "/hello"
        }
      ]
    }
  ]
}
